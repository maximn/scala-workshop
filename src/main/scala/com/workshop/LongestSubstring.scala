package com.workshop

//longest substring that contains uppercase&lowercase but not numbers
// aV3rwerwr6Wix -> Wix
object LongestSubstring {
  // split - regex "[0-9]" to split by num
  // filter - filter strings that have (exists) upper & lower.
  // maxBy - take longest
  def find(str: String): String = ???
}
